# monopanel · модуль «Домены» (`apps/domains`)

Модуль массового отбора доменов для панели **monopanel** (Docker-монорепо, рядом
с `apps/cf`, `apps/content`). Реализует **единый двухступенчатый пайплайн**,
собранный из двух ТЗ в одну фронтенд-панель: оба инструмента видны в одном окне
(без переключения вкладок), настройки и история — в выезжающих панелях.

```
Выгрузка Ahrefs Batch Analysis
        │
        ▼
┌─────────────────────────────┐     мост      ┌──────────────────────────────┐
│  Часть 1: Ahrefs-фильтр      │  A/B/C →      │  Часть 2: Классификатор       │
│  количественный гейт         │  Targets  →   │  по имени (нейросеть)         │
│  авторитет / спам / тиры     │───────────────▶  pharma/casino/adult          │
│  чистая функция, без API     │               │  good / bad / на проверку     │
└─────────────────────────────┘               └──────────────────────────────┘
        Часть 1 — реализована                    Часть 2 — реализована
```

**Мост** — это суть связки: Часть 1 отсеивает мусор по ссылочному профилю и
раскладывает выжившие домены по тирам; кнопкой «Отправить тиры A/B/C →
Классификатор» их `Target` уезжают во вторую часть, где нейросеть судит уже по
названию. Две половины ТЗ — это две последовательные стадии одного отбора,
при этом каждый инструмент полностью работает и независимо.

Панель на **порту 43784**, светлая тема.

---

## Статус

| Часть | Что | Статус |
|---|---|---|
| 1 | Ahrefs количественный фильтр (парсинг, отсев, скор, тиры, флаги, экспорт, живой пересчёт, пресеты) | ✅ реализовано + тесты |
| Мост | Передача Targets выбранных тиров из Части 1 в Часть 2 | ✅ реализовано |
| Оболочка | Одно окно на оба инструмента, светлая тема, вкладка «Настройки» с 3 секциями (Scoring / AI / Ключ OpenAI), выезжающие панели настроек и истории | ✅ реализовано |
| 2 | Классификатор по имени (OpenAI/OpenRouter, батчи, JSON-схема, ретраи, бакеты, кэш, история/логи, стоимость) | ✅ реализовано + тесты |
| Корректировки | Раскладка по вердикту, `dr_floor`, выбор модели (`gpt-4.1-mini` + резервы) | ✅ применены (см. ниже) |

Детали архитектуры и связки двух ТЗ — в [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md).

## Корректировки (по результатам тестов)

Аддендумы к ТЗ, имеющие приоритет над исходными пунктами:

- **Раскладка классификатора — строго по вердикту:** `error`→На проверку,
  `bad`→Bad, `good`→Good. Язык имени и `confidence` больше **не влияют** на
  бакет — только справочные колонки. Чистое имя на любом языке → Good.
- **Инструкция модели усилена:** распознавание терминов в склеенных строках, с
  аффиксами и транслитом (`wangdai`=网贷), бренды препаратов (`avana`), лексиконы
  pharma/crypto_finance; защита от ложных срабатываний (`cialico`≠cialis).
- **Анализатор — правило `dr_floor`:** REJECT если `DR < dr_min` (дефолт **0.5**)
  — ловит DR=0 с наполненным ссылочным. Плюс опциональное `spam_floor_reject`
  (тумблер, по умолчанию выкл).
- **Выбор модели классификатора:** дефолт `gpt-4.1-mini`, резервы
  `gpt-4.1-nano` / `gpt-5.4-mini` / `gpt-5.4-nano` + ручной ввод. Селектор — только
  у классификатора (анализатор нейросеть не использует). Порог confidence убран.

---

## Структура

```
monopanel/
├─ docker-compose.yml         порт 43784, авто-имена контейнеров
├─ reset.sh                   чистый пересбор (сносит старые контейнеры)
├─ docs/ARCHITECTURE.md
└─ apps/domains/
   ├─ backend/                  FastAPI (Python 3.11)
   │  ├─ app/
   │  │  ├─ config_defaults.py  дефолтный конфиг (§9 ТЗ-1)
   │  │  ├─ ahrefs/             чистое ядро Части 1
   │  │  │  ├─ loader.py        чтение UTF-16/таб и UTF-8/запятая, дедуп, валидация
   │  │  │  ├─ parsing.py       нормализация чисел, cc, tld (§3)
   │  │  │  ├─ metrics.py       производные метрики (§4)
   │  │  │  ├─ reject.py        жёсткий отсев (§5)
   │  │  │  ├─ scoring.py       композитный скор (§6)
   │  │  │  ├─ flags.py         флаги (§8)
   │  │  │  ├─ pipeline.py      тиры + сборка результата (§7, §10)
   │  │  │  ├─ export.py        CSV / TXT (§10)
   │  │  │  └─ config.py        merge + валидация (§9)
   │  │  ├─ classifier/         ядро Части 2
   │  │  │  ├─ normalize.py     нормализация/валидация доменов (FR-1..4)
   │  │  │  ├─ instruction.py   фиксированная инструкция модели + JSON-схема (FR-7,9)
   │  │  │  ├─ schema.py        валидация/коэрция ответа модели (FR-9,10)
   │  │  │  ├─ buckets.py       раскладка по вердикту good/bad/review (Корр. 1)
   │  │  │  ├─ providers.py     OpenAI-совместимый провайдер + Mock + verify (FR-6)
   │  │  │  ├─ engine.py        батчи, concurrency, ретраи, кэш, отмена (FR-5,8,12)
   │  │  │  ├─ cost.py          оценка стоимости, Batch −50% (NFR-3)
   │  │  │  ├─ settings_defaults.py  дефолт модели + список моделей
   │  │  │  └─ store.py         настройки/прогоны/результаты/логи/кэш (SQLite)
   │  │  ├─ api/                HTTP-роуты (обе части)
   │  │  ├─ store.py            датасеты в памяти + пресеты в SQLite (Часть 1)
   │  │  └─ main.py
   │  └─ tests/                 31 тест на числах/правилах из ТЗ и корректировок
   └─ frontend/                 React + Vite + TypeScript
      └─ src/
         ├─ App.tsx             одно окно на оба инструмента + выезжающие панели
         ├─ ahrefs/            UI Части 1 (загрузка, сводка, таблица, экспорт, гайд)
         ├─ settings/          вкладка настроек: Scoring / AI / Ключ OpenAI
         ├─ classifier/        UI Части 2 (Проверка, бакеты, прогресс, экспорт, гайд)
         └─ history/           история прогонов и логи
```

---

## Запуск

### Через Docker (как в monopanel)
```bash
docker compose up --build -d
# панель:  http://localhost:43784
```

Одной командой на macOS (клонирует на рабочий стол, собирает, открывает):
```bash
cd ~/Desktop && git clone https://github.com/MaksimCH-afk/domainchecker.git \
  && cd domainchecker && docker compose up --build -d && sleep 6 \
  && open http://localhost:43784
```

Если старый контейнер «залип» (например, остался на другом порту) — чистый
пересбор со сносом старых контейнеров:
```bash
./reset.sh
```

Для прогона классификатора задайте ключ в панели: **Настройки → Ключ OpenAI**
(кнопка «Проверить» валидирует ключ). Модель — **Настройки → AI**. Часть 1
(Ahrefs-фильтр) работает сразу, без ключей.

### Локальная разработка
```bash
# backend
cd apps/domains/backend
python3 -m venv .venv && . .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload            # :8000

# frontend (в другом терминале)
cd apps/domains/frontend
npm install
npm run dev                              # :5173, проксирует /api на :8000
```

### Тесты
```bash
cd apps/domains/backend && . .venv/bin/activate && pytest -q
```

---

## Ключевые принципы реализации Части 1

- **Ранжирование, не бинарный отсев.** Жёстко режется только явный мусор (~6%);
  всё остальное скорится, а финальную границу ставит пользователь порогом/топ-N.
- **Чистая функция.** `analyze(records, config)` не имеет побочных эффектов —
  поэтому смена любого параметра в «Настройках» пересчитывает результат
  **без повторной загрузки** (§9): бэкенд держит распарсенные строки в памяти
  и лишь заново прогоняет пайплайн.
- **Всё в конфиге.** Веса, cap-константы, пороги отсева, границы тиров, наборы
  флагов — редактируются из UI, валидируются (веса ≥ 0, тиры монотонны),
  сохраняются в пресеты.
- **Загрузчик — мост форматов.** Ahrefs отдаёт один и тот же экспорт в двух
  формах (UTF-16/таб, UTF-8/запятая); `loader.py` приводит обе к одному
  набору полей по **имени заголовка**, а не по индексу.

## API (Часть 1)

| Метод | Путь | Назначение |
|---|---|---|
| POST | `/api/ahrefs/datasets` | загрузка файла выгрузки |
| POST | `/api/ahrefs/datasets/rows` | приём уже загруженных строк (JSON) |
| POST | `/api/ahrefs/datasets/{id}/analyze` | пересчёт под конфиг |
| POST | `/api/ahrefs/datasets/{id}/export` | CSV / TXT |
| POST | `/api/ahrefs/datasets/{id}/targets` | **мост**: Targets выбранных тиров |
| GET  | `/api/config/default` | дефолтный конфиг |
| POST | `/api/config/validate` | валидация (предупреждения) |
| GET/POST/DELETE | `/api/config/presets…` | пресеты |

## API (Часть 2)

| Метод | Путь | Назначение |
|---|---|---|
| POST | `/api/classify/preview` | счётчики распознано/дубли/невалидные |
| POST | `/api/classify/runs` | старт прогона (фоновый async) |
| GET  | `/api/classify/runs/{id}` | прогресс + бакеты (инкрементально) |
| POST | `/api/classify/runs/{id}/cancel` | отмена |
| GET  | `/api/classify/runs` | история прогонов |
| GET  | `/api/classify/runs/{id}/logs` | логи прогона |
| POST | `/api/classify/runs/{id}/export` | CSV / TXT (бакет или всё) |
| GET/PUT | `/api/classify/settings` | AI-настройки + список моделей (ключи маскируются) |
| POST | `/api/classify/settings/verify` | проверка API-ключа (тест к провайдеру) |

> **Провайдеры.** OpenAI (основной) и OpenRouter (резерв) — через единый
> OpenAI-совместимый клиент; ключи задаются в «Настройках» и хранятся
> на стороне панели (маскируются, не в логах). Дефолтная модель классификатора —
> `gpt-4.1-mini`, резервы `gpt-4.1-nano` / `gpt-5.4-mini` / `gpt-5.4-nano` (список
> в UI + ручной ввод). Провайдер `mock` — офлайн-демо без сети/ключей.
