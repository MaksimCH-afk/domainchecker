import { useState } from "react";

// User guide embedded on the AI classifier page.
export function ClassifierGuide() {
  const [open, setOpen] = useState(false);
  return (
    <div className="guide">
      <button className="guide-toggle" onClick={() => setOpen(!open)}>
        📖 Руководство пользователя: ИИ-Классификатор доменов {open ? "▲" : "▼"}
      </button>
      {open && (
        <div className="guide-body">
          <p>
            Модуль для массовой и быстрой проверки больших списков доменов. Он
            анализирует <b>только название</b> домена (не заходя на сайт) и отделяет
            чистые имена от «мусора» (казино, adult, фарма, скам и т.п.). Всю
            интеллектуальную работу выполняет нейросеть — без заготовленных словарей.
          </p>

          <h4>📌 Главные принципы</h4>
          <ul>
            <li><b>Анализируется только имя:</b> ИИ ищет проблемные термины и паттерны — в т.ч. вклеенные в строку, с аффиксами и транслитом.</li>
            <li><b>Решает только нейросеть:</b> она сама определяет категорию, язык и свою уверенность.</li>
            <li><b>Раскладка — строго по вердикту (категории):</b> язык имени и уверенность на корзину <u>не влияют</u> — это лишь справочные колонки в таблице.</li>
          </ul>

          <h4>🗂 Три корзины (бакета)</h4>
          <ol>
            <li><b>Good:</b> чистые имена — <b>на любом языке</b> (бренды, фамилии, топонимы, иностранные и даже короткие непрозрачные имена вроде <code>bzdiao</code>). Основной рабочий материал.</li>
            <li><b>Bad:</b> название содержит проблемный термин; нейросеть подписывает категорию (фарма, adult, гемблинг, крипта, скам, пиратство и т.д.) — независимо от языка.</li>
            <li>
              <b>На проверку (Review):</b> в норме почти пуст — сюда попадают только
              технические сбои: невалидный формат домена, ошибка API или редкий
              случай, когда модель явным флагом сообщает, что не может выбрать.
            </li>
          </ol>

          <h4>⚙️ Как пользоваться</h4>
          <p>
            <b>Шаг 1.</b> Вставьте список доменов в текстовое поле (по одному в
            строке). Система очистит их (<code>https://</code>, <code>www</code>,
            пути) и уберёт дубликаты.
          </p>
          <p>
            <b>Шаг 2.</b> Нажмите «Проверить». Список разбивается на пакеты и
            отправляется нейросети; на экране виден прогресс.
          </p>
          <p>
            <b>Шаг 3.</b> Переключайтесь между бакетами. В таблице — домен,
            вердикт, категория, язык и причина. В «На проверку» есть фильтр по
            языку.
          </p>
          <p>
            <b>Шаг 4.</b> «Выделить всё и скопировать» забирает домены из вкладки;
            доступна выгрузка в CSV (полная таблица) или TXT (список).
          </p>

          <h4>🎛 Настройки и История</h4>
          <p>
            Правила раскладки зашиты жёстко, но вы управляете техникой: выбор
            провайдера и <b>модели классификатора</b> — по умолчанию стоит
            рекомендованная <code>gpt-4.1-mini</code>, в списке есть резервные
            (<code>gpt-4.1-nano</code>, <code>gpt-5.4-mini</code>,
            <code> gpt-5.4-nano</code>) и ручной ввод. Также — Batch API (−50%) и
            параметры прогона (размер пакета, параллельность, ретраи).
          </p>
          <p>
            <b>История проверок</b> сохраняется автоматически: в «Истории и логах»
            можно открыть прошлый запуск, посмотреть статистику и стоимость,
            заново скачать результаты. При обрыве интернета уже обработанная часть
            не теряется.
          </p>
        </div>
      )}
    </div>
  );
}
